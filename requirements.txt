# --- PyTorch (GPU Version - Stable CUDA 12.4) ---
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1

# --- Core Computer Vision ---
ultralytics>=8.3.0    # The Framework
opencv-python>=4.10.0 # Video Processing
numpy>=1.24.0         # Math

# --- Tracking & Slicing ---
supervision>=0.24.0   # Contains ByteTrack & InferenceSlicer

# --- CRITICAL: TensorRT & Export Support ---
tensorrt>=10.0.0      # <--- REQUIRED to run .engine files
tensorrt-libs>=10.0.0 # Libraries for the bindings
onnx>=1.17.0          # Required for .pt -> .engine conversion
onnxruntime-gpu>=1.19.0 # Fallback acceleration

# --- Utilities ---
PyYAML>=6.0
requests>=2.31.0
tqdm>=4.66.0
